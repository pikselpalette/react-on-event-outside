language: node_js
node_js: 10
jobs:
  include:
    - stage: build
      script:
        - NODE_ENV=production npm run build
    - stage: lint
      script:
        - npm run lint
    - stage: test
      script:
        - npm run test
        - npm run test:mutate
